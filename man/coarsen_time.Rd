% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatiotemporal.R
\name{coarsen_time}
\alias{coarsen_time}
\title{put timestamps onto a coarser grid}
\usage{
coarsen_time(x, resolution = 1)
}
\arguments{
\item{x}{numeric timestamps, or anything [as.numeric()] turns into them
(a POSIXct becomes seconds since the epoch, so `resolution` is then in
seconds)}

\item{resolution}{width of one bin, in the units of `x`. `1` leaves `x`
alone up to flooring; `24` on hourly data gives days.}
}
\value{
an integer-valued numeric vector of bin indices
}
\description{
put timestamps onto a coarser grid
}
\examples{
coarsen_time(c(0, 1, 5, 23, 24, 25), resolution = 12)

}
