% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knowledge.R
\name{reid_knowledge_curve}
\alias{reid_knowledge_curve}
\title{compare reidentification risk across the W / M / S knowledge levels}
\usage{
reid_knowledge_curve(
  dat_raw_anon,
  quasi_identifiers,
  behavior = NULL,
  identifiers = NULL,
  weak_subset = NULL,
  levels = c("W", "M", "S"),
  row_number = "ROW_NUMBER",
  seeds = 1:20,
  ...
)
}
\arguments{
\item{dat_raw_anon}{dataframe of raw_anon form}

\item{quasi_identifiers, behavior, identifiers, weak_subset}{passed to
[attacker_knowledge()]}

\item{levels}{which knowledge levels to evaluate (default all three, in
increasing order of knowledge)}

\item{row_number}{name of the row-number column before RAW_/ANON_ prefixing}

\item{seeds}{tie-break seeds passed to [reid_evaluate()]}

\item{...}{further arguments passed to [score_by_knowledge()]}
}
\value{
a data frame with one row per level and columns `level`,
  `n_visible`, `success_analytic`, `success_mean`, `success_sd`,
  `baseline_random`, `lift` and `max_risk`
}
\description{
Runs the same attack machinery three times on the same data, each time
restricted to what that knowledge level allows the attacker to see, and
reports the resulting risk side by side. This is the form the knowledge
model is meant to be read in: a single number for "the" reidentification
rate is only interpretable once the assumed adversary is stated.
}
\examples{
d <- create_dummy_qi_data(people = 25, seed = 1)
j <- join_raw_anon_data(d, d)
reid_knowledge_curve(
  j,
  quasi_identifiers = c(AGE = "num", ZIP = "char", SEX = "char"),
  behavior = c(VISIT_COUNT = "num", SPEND_MEAN = "num"),
  identifiers = c(FINGERPRINT = "num"),
  weak_subset = "ZIP",
  seeds = 1:5
)

}
