% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalize.R
\name{non_numeric_target_message}
\alias{non_numeric_target_message}
\title{the error message for a target column that has to be numeric and is not}
\usage{
non_numeric_target_message(
  dat_raw_anon,
  cols,
  target,
  fn_name,
  alternative,
  units = generalization_units()
)
}
\arguments{
\item{dat_raw_anon}{dataframe of raw_anon form}

\item{cols}{resolved column names from [reid_prefixed_columns()]}

\item{target}{the target name as the caller wrote it}

\item{fn_name}{the user-facing function name}

\item{alternative}{what to suggest when the column is *not* generalised}

\item{units}{unit strings, see [generalization_units()]}
}
\value{
a single string
}
\description{
Named separately from [check_generalized_target()] because these callers
cannot continue at all: there is no arithmetic to do on a character column,
so there is no `"warn"` to offer. The message still runs the generalisation
check, because "this column is character" and "this column is a set of
published regions" call for completely different fixes.
}
\keyword{internal}
