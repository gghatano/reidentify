% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setsim.R
\name{side_token_sets}
\alias{side_token_sets}
\title{build the per-record token sets of one side of a RAW/ANON candidate table}
\usage{
side_token_sets(
  dat_raw_anon,
  row_col,
  target_col,
  split,
  multiset,
  target,
  fn_name
)
}
\arguments{
\item{dat_raw_anon}{dataframe of raw_anon form}

\item{row_col}{name of the (already prefixed) row-number column}

\item{target_col}{name of the (already prefixed) target column}

\item{split}{literal separator}

\item{multiset}{keep repeated elements (`TRUE`) or reduce to a true set
(`FALSE`)}

\item{target, fn_name}{used in error messages}
}
\value{
a list with `sets` (a list of character vectors, one per distinct
  record) and `index` (for every row of `dat_raw_anon`, the position in
  `sets` of that row's record)
}
\description{
The candidate table is a cross join, so every record's collapsed value
appears once per candidate pair. Splitting it once per *record* and then
broadcasting back onto the pair rows is the same trick
[compute_num_ranks()] uses for ranks, and keeps the cost linear in the
number of records rather than the number of pairs.
}
\keyword{internal}
