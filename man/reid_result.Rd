% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reidentify.R
\name{reid_result}
\alias{reid_result}
\title{craete text of reidentify result ( method: ******, success/trial : ***** / ******)}
\usage{
reid_result(
  dat_reid_result,
  raw_row_number = "RAW_ROW_NUMBER",
  anon_row_number = "ANON_ROW_NUMBER",
  result = "RESULT",
  method = NULL
)
}
\arguments{
\item{dat_reid_result}{reid result data frame (RAW_ROW_NUMBER, ANON_ANON_NUMBER, RESULT)}

\item{raw_row_number}{column name of row number in RAW data}

\item{anon_row_number}{column name of row number in ANON data}

\item{result}{true or false}

\item{method}{reid method name}
}
\value{
a character scalar of the form
  `" method: <method> , success / trial :  <success> / <trial> "`,
  where `trial` is the number of rows in `dat_reid_result` and
  `success` is the number of TRUE values in its `result` column;
  `success` is always <= `trial`.
}
\description{
craete text of reidentify result ( method: ******, success/trial : ***** / ******)
}
\examples{
raw  <- data.frame(ROW_NUMBER = 1:5, V = c(10, 20, 30, 40, 50))
anon <- data.frame(ROW_NUMBER = 1:5, V = c(11, 19, 33, 38, 52))
d <- join_raw_anon_data(raw, anon)
reid_result(reid_by_num(d, "V"), method = "reid_by_num")

# it also accepts the assignment layer's output, whose columns already
# carry the default names
reid_result(match_greedy(score_num(d, "V")), method = "match_greedy")

}
