% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reidentify.R
\name{with_local_seed}
\alias{with_local_seed}
\title{evaluate `code` with the RNG seeded to `seed`, restoring the caller's
RNG state afterwards}
\usage{
with_local_seed(seed, code)
}
\arguments{
\item{seed}{integer seed, or NULL to use the ambient RNG stream}

\item{code}{expression to evaluate}
}
\value{
the value of `code`
}
\description{
`seed = NULL` runs `code` against the ambient RNG stream unchanged, so a
caller can still get reproducibility with a plain `set.seed()` before the
call. Any other value makes the call self-contained and repeatable without
perturbing the caller's stream.
}
\keyword{internal}
