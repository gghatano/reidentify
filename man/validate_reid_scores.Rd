% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{validate_reid_scores}
\alias{validate_reid_scores}
\title{check that `x` looks like a score table, and return its orientation}
\usage{
validate_reid_scores(x, arg = "scores")
}
\arguments{
\item{x}{object to validate}

\item{arg}{argument name used in the error message}
}
\value{
the score type of `x`, one of "distance" or "similarity"
}
\description{
Accepts any data frame carrying the three score-layer columns, so callers
can build score tables by hand or pipe one through dplyr (which drops the
class and attributes). A table with no recorded orientation is treated as
"distance", the package-wide default documented in `new_reid_scores()`.
}
\keyword{internal}
