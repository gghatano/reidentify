% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalize.R
\name{gen_unit_patterns}
\alias{gen_unit_patterns}
\title{the two unit patterns, built once per distinct `units` vector}
\usage{
gen_unit_patterns(units)
}
\arguments{
\item{units}{unit strings, see [generalization_units()]}
}
\value{
a list with `trailing` and `after_digit` regular expressions, or
  NULL when `units` is empty
}
\description{
The units are alternated longest-first so that `"yrs"` is not read as
`"y"` + `"rs"` under either leftmost-longest or leftmost-first matching, and
the result is cached: [parse_generalized_interval()] is called once per
distinct value of a column, and rebuilding a 30-branch alternation each time
dominated the cost.
}
\keyword{internal}
