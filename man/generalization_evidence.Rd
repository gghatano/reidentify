% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalize.R
\name{generalization_evidence}
\alias{generalization_evidence}
\title{pairs showing that a target column is generalised on the ANON side}
\usage{
generalization_evidence(
  raw_vals,
  anon_vals,
  units = generalization_units(),
  max_examples = 3L,
  min_share = GENERALIZATION_SHARE_THRESHOLD
)
}
\arguments{
\item{raw_vals, anon_vals}{the two sides of one column of a candidate table}

\item{units}{unit strings, see [generalization_units()]}

\item{max_examples}{how many distinct example pairs to keep for the message}

\item{min_share}{the share of non-missing ANON entries that must be regions}
}
\value{
a data frame with columns `RAW` and `ANON`, with zero rows when
  there is no evidence. The share actually observed is attached as the
  `share` attribute.
}
\description{
Requires two independent facts, because either alone misfires: enough of the
ANON side has to be region-shaped (`min_share`), *and* some RAW value has to
fall inside one of those regions without being equal to it. Without the
second, `"37"` against `"37.0"` would be reported as a generalisation;
without the first, one accidental `"0s"` in a column of random codes would.
}
\keyword{internal}
