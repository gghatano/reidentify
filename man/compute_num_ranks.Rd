% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{compute_num_ranks}
\alias{compute_num_ranks}
\title{compute the per-row RAW/ANON ranks used by score_num_rank()}
\usage{
compute_num_ranks(
  dat_raw_anon,
  target,
  row_number,
  fn_name,
  generalized = c("stop", "warn", "ignore")
)
}
\arguments{
\item{dat_raw_anon}{dataframe of raw_anon form}

\item{target}{target column}

\item{row_number}{name of the row-number column *before* the RAW_/ANON_
prefixing done by [join_raw_anon_data()] (default: "ROW_NUMBER")}

\item{fn_name}{name used in error messages}

\item{generalized}{`"stop"`, `"warn"` or `"ignore"`; see [score_num_rank()]}
}
\value{
a list with the per-candidate-row vectors `raw_row_number`,
  `anon_row_number`, `raw_rank`, `anon_rank`, plus the resolved column
  names in `cols`.
}
\description{
Split out from [score_num_rank()] so that a caller which wants the two rank
columns themselves does not have to recompute them.
}
\details{
Ranks are computed over the *distinct* (row number, target) pairs on each
side -- i.e. once per record, not once per candidate pair -- and then
broadcast back onto every candidate row.
}
\keyword{internal}
