% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalize.R
\name{containment_vector}
\alias{containment_vector}
\title{containment of every RAW value in every ANON value, for one target}
\usage{
containment_vector(raw_vals, anon_vals, attribute, hierarchy, rule, units)
}
\arguments{
\item{raw_vals, anon_vals}{per-row character vectors}

\item{attribute}{target name, used to select the hierarchy}

\item{hierarchy}{a "reid_hierarchy" object, or NULL}

\item{rule}{containment rule}

\item{units}{unit strings}
}
\value{
logical vector, one per row, carrying a `dead_nodes` attribute: the
  published values that excluded *every* RAW value, each tagged with how it
  was read
}
\description{
Computed over the *distinct* values on each side and then broadcast back
onto the candidate rows: a cross join repeats the same few generalised
values thousands of times, and parsing "30代" once per pair would dominate
the cost.
}
\keyword{internal}
