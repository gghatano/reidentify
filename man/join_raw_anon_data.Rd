% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_dummy_data.R
\encoding{UTF-8}
\name{join_raw_anon_data}
\alias{join_raw_anon_data}
\title{create reid-format data from raw and anon data frame}
\usage{
join_raw_anon_data(raw, anon, raw_header = "RAW_", anon_header = "ANON_")
}
\arguments{
\item{raw}{raw data frame}

\item{anon}{anonymized data frame}

\item{raw_header}{strings which is added for columns from raw data}

\item{anon_header}{strings which is added for columns from anon data}
}
\value{
a data frame: the cross join (every RAW row paired with every
  ANON row) of `raw` and `anon`, with their column names prefixed by
  `raw_header`/`anon_header` respectively.
}
\description{
create reid-format data from raw and anon data frame
}
\examples{
raw  <- data.frame(ROW_NUMBER = 1:3, V = c(10, 20, 30))
anon <- data.frame(ROW_NUMBER = 1:3, V = c(11, 19, 32))

# every RAW row is paired with every ANON row: 3 x 3 = 9 candidate pairs.
# This is the shape the score_*() / reid_by_*() functions expect.
d <- join_raw_anon_data(raw, anon)
dim(d)
head(d, 3)

}
