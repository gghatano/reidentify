# 敵対的検証の再現スクリプト（アーカイブ）

2026-07-31 に実施した敵対的検証で使用したスクリプトと実行ログ。
**このブランチは記録用で、`master` には統合しない。**

| ディレクトリ | 観点 | 主な発見 |
|---|---|---|
| `adv1/` | 過小報告を作らせる | #57 `combine_scores()` の非正規化、#59 Mahalanobis の逆効果、#56 `n_true_missing` の沈黙 |
| `adv2/` | 正しさそのものを壊す | #56 BLOCKED 検出の死角、#60 重複候補ペア、#61 浮動小数点の同点判定、#58 unicity のキー衝突 |
| `adv3/` | 検証基盤を疑う | #62 ハーネスの静かなスキップ、#63 公開ページの再陳腐化、#64 ミューテーション kill rate 45.1%、#65 CI の死にコード |

## 注意

- **これらのスクリプトは、当時の `master`（`3f6d2f0` 前後）に対して書かれている。** その後の修正で API が変わっているため、そのままでは動かないものがある
- 発見された欠陥はすべて修正済みで、回帰テストが入っている。**このアーカイブは「どう見つけたか」の記録であって、現在のリスクを示すものではない**
- ミューテーション試験の生成物（`adv3/gen-mutants.R` など）は `R/` を書き換える。**実行する場合は必ず隔離した worktree で行うこと**
